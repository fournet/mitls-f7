(* ------------------------------------------------------------------------ *)
let hostname = "pierre-yves.strub.nu"

(* ------------------------------------------------------------------------ *)
let main () =
    let channel = MiHTTPChannel.create hostname in
        printfn "Hello World!"

(* ------------------------------------------------------------------------ *)
let () = main ()
