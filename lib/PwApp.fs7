module PwApp

(*
predicate Authenticated of string
predicate AuthenticatedWithTk of token * string

definition !name, tk. AutheneticatedWithTk(tk, name) => Authenticated(name)

val request : name:string -> my:string -> token:token -> b:bool{
  b = true => ?si. CN(si.serverID) = name /\ (Strong(si.cipher_suite) /\ Honest(si.serverID) => Authenticated(my)) 
}

val response : unit -> name:string{
  ?si, token. Strong(si.cipher_suite) => AuthenticatedWithTk(name, token)
} option
*)
