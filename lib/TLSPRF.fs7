module TLSPRF

(* Concrete (bytes -> bytes) PRF algorithms for TLS; trusted. *)
(* Used by CRE and PRF *) 

open Bytes
open TLSConstants
open TLSInfo

val verifyData: prfAlg -> bytes -> Role -> bytes -> bytes 
val extract:    prfAlg -> bytes -> bytes -> l:nat -> (;l)lbytes
val kdf:        prfAlg -> bytes -> bytes -> l:nat -> (;l)lbytes

(* SSL-specific certificate verify *)
val ssl_certificate_verify: bytes -> bytes -> hashAlg -> bytes